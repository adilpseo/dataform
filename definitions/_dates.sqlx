config {
  type: "table"
}

SELECT Client, start_date, d._date
FROM ${ref("master_sheet")} m
JOIN (SELECT * FROM UNNEST(GENERATE_DATE_ARRAY('2023-01-01', CURRENT_DATE, INTERVAL 1 DAY)) AS _date) d ON d._date >= m.start_date
